# Руководство по разработке в Cursor

Пошаговое руководство для начала разработки проекта в Cursor IDE.

## 🎯 Перед началом

### Что уже готово

✅ **Полная архитектура проекта**
- Все модули спроектированы
- Код написан и документирован
- Тесты подготовлены
- Docker конфигурация готова

✅ **Документация**
- README.md - основная документация
- QUICKSTART.md - быстрый старт
- EXAMPLES.md - примеры использования
- FAQ.md - частые вопросы
- ARCHITECTURE.md - архитектура

### Что нужно сделать

🔨 **Ваша задача:**
1. Создать структуру проекта
2. Скопировать код из артефактов
3. Установить зависимости
4. Протестировать
5. Начать разработку расширенной версии (v1.1)

---

## 📁 Шаг 1: Создание структуры проекта

### 1.1 Создайте корневую папку

```bash
mkdir youtube-transcriber
cd youtube-transcriber
```

### 1.2 Создайте структуру директорий

```bash
# Основные папки
mkdir -p src tests output temp logs models/whisper models/nllb

# Файлы
touch src/__init__.py
touch src/main.py
touch src/config.py
touch src/logger.py
touch src/utils.py
touch src/downloader.py
touch src/transcriber.py
touch src/translator.py
touch src/document_writer.py

touch tests/__init__.py
touch tests/test_utils.py
touch tests/test_transcriber.py

# Конфигурационные файлы
touch requirements.txt
touch requirements-dev.txt
touch .env.example
touch .gitignore
touch Dockerfile
touch docker-compose.yml
touch setup.py

# Документация
touch README.md
touch QUICKSTART.md
touch EXAMPLES.md
touch FAQ.md
touch ARCHITECTURE.md
```

### 1.3 Проверьте структуру

```bash
tree -L 2
```

Должно получиться:
```
youtube-transcriber/
├── src/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   ├── logger.py
│   ├── utils.py
│   ├── downloader.py
│   ├── transcriber.py
│   ├── translator.py
│   └── document_writer.py
├── tests/
│   ├── __init__.py
│   ├── test_utils.py
│   └── test_transcriber.py
├── output/
├── temp/
├── logs/
├── models/
├── requirements.txt
├── requirements-dev.txt
├── .env.example
├── .gitignore
├── Dockerfile
├── docker-compose.yml
├── setup.py
└── README.md
```

---

## 📋 Шаг 2: Копирование кода

### 2.1 Скопируйте код из артефактов

Я создал все необходимые файлы как артефакты. Скопируйте содержимое каждого артефакта в соответствующий файл:

**Основные модули:**
1. `src/__init__.py` ← из артефакта "src/__init__.py"
2. `src/config.py` ← из артефакта "src/config.py"
3. `src/logger.py` ← из артефакта "src/logger.py"
4. `src/utils.py` ← из артефакта "src/utils.py"
5. `src/downloader.py` ← из артефакта "src/downloader.py"
6. `src/transcriber.py` ← из артефакта "src/transcriber.py"
7. `src/translator.py` ← из артефакта "src/translator.py"
8. `src/document_writer.py` ← из артефакта "src/document_writer.py"
9. `src/main.py` ← из артефакта "src/main.py"

**Тесты:**
10. `tests/test_utils.py` ← из артефакта "tests/test_utils.py"
11. `tests/test_transcriber.py` ← из артефакта "tests/test_transcriber.py"

**Конфигурация:**
12. `requirements.txt` ← из артефакта "requirements.txt"
13. `requirements-dev.txt` ← из артефакта "requirements-dev.txt"
14. `.env.example` ← из артефакта ".env.example"
15. `.gitignore` ← из артефакта ".gitignore"
16. `Dockerfile` ← из артефакта "Dockerfile"
17. `docker-compose.yml` ← из артефакта "docker-compose.yml"
18. `setup.py` ← из артефакта "setup.py"

**Документация:**
19. `README.md` ← из артефакта "README.md"
20. `QUICKSTART.md` ← из артефакта "QUICKSTART.md"
21. `EXAMPLES.md` ← из артефакта "EXAMPLES.md"
22. `FAQ.md` ← из артефакта "FAQ.md"
23. `ARCHITECTURE.md` ← из артефакта "ARCHITECTURE.md"

### 2.2 Создайте tests/__init__.py

```python
# tests/__init__.py
"""
Тесты для YouTube Transcriber
"""
```

---

## 🔧 Шаг 3: Настройка окружения в Cursor

### 3.1 Откройте проект в Cursor

```bash
cursor youtube-transcriber
```

или через GUI: File → Open Folder

### 3.2 Создайте виртуальное окружение

В терминале Cursor (Ctrl+`):

```bash
# Создание venv
python -m venv venv

# Активация (macOS/Linux)
source venv/bin/activate

# Активация (Windows)
# venv\Scripts\activate
```

### 3.3 Настройте Python interpreter в Cursor

1. Cmd+Shift+P (macOS) или Ctrl+Shift+P (Windows/Linux)
2. Найдите "Python: Select Interpreter"
3. Выберите `./venv/bin/python`

### 3.4 Установите FFmpeg

**macOS:**
```bash
brew install ffmpeg
```

**Linux:**
```bash
sudo apt update && sudo apt install ffmpeg
```

**Windows:**
- Скачайте с ffmpeg.org
- Добавьте в PATH

### 3.5 Установите зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
pip install -r requirements-dev.txt  # Для разработки
```

⏱️ Это займет 5-10 минут

---

## 🧪 Шаг 4: Первый запуск и тестирование

### 4.1 Проверьте импорты

```bash
python -c "import src; print('OK')"
```

### 4.2 Запустите справку

```bash
python -m src.main --help
```

Должна появиться справка по использованию.

### 4.3 Запустите тесты

```bash
# Все тесты
pytest tests/

# С coverage
pytest --cov=src tests/

# Verbose mode
pytest -v tests/
```

### 4.4 Первый реальный тест

Используйте короткое тестовое видео:

```bash
python -m src.main \
    --url "https://www.youtube.com/watch?v=dQw4w9WgXcQ" \
    --transcribe whisper_base
```

⚠️ **Важно:** Первый запуск загрузит модели (~2-3GB)

---

## 🔍 Шаг 5: Работа в Cursor

### 5.1 Полезные расширения Cursor

Рекомендуемые расширения:
- Python (Microsoft)
- Pylance
- Black Formatter
- autoDocstring
- GitLens

### 5.2 Настройки форматирования

Создайте `.vscode/settings.json`:

```json
{
    "python.linting.enabled": true,
    "python.linting.pylintEnabled": false,
    "python.linting.flake8Enabled": true,
    "python.formatting.provider": "black",
    "editor.formatOnSave": true,
    "python.testing.pytestEnabled": true,
    "python.testing.unittestEnabled": false,
    "[python]": {
        "editor.rulers": [88],
        "editor.tabSize": 4
    }
}
```

### 5.3 Использование Cursor AI

**Полезные промпты для Cursor:**

1. **Понимание кода:**
```
Explain how the transcriber.py module works
```

2. **Рефакторинг:**
```
Refactor the translate_text method to improve readability
```

3. **Добавление функций:**
```
Add support for processing local audio files to main.py
```

4. **Документация:**
```
Generate docstrings for all methods in utils.py
```

5. **Тесты:**
```
Write unit tests for the sanitize_filename function
```

### 5.4 Debugging в Cursor

Создайте `.vscode/launch.json`:

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Main Module",
            "type": "python",
            "request": "launch",
            "module": "src.main",
            "args": [
                "--url", "https://youtube.com/watch?v=...",
                "--transcribe", "whisper_base"
            ],
            "console": "integratedTerminal"
        },
        {
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal"
        },
        {
            "name": "Python: Pytest",
            "type": "python",
            "request": "launch",
            "module": "pytest",
            "args": [
                "tests/",
                "-v"
            ],
            "console": "integratedTerminal"
        }
    ]
}
```

---

## 🚀 Шаг 6: Начало разработки v1.1

### Roadmap для v1.1

**Приоритетные задачи:**

1. ✅ **Обработка локальных аудиофайлов**
   - Добавить поддержку `--input_audio`
   - Поддержка форматов: mp3, wav, m4a, flac

2. ✅ **Обработка текстовых файлов**
   - Добавить поддержку `--input_text`
   - Парсинг docx через python-docx
   - Парсинг markdown

3. ✅ **Whisper Small модель**
   - Добавить в TranscribeOptions
   - Обновить загрузку модели

4. ✅ **Улучшенное логирование**
   - Добавить цвета в консольный вывод
   - Структурированные логи (JSON option)

### План работы

**Неделя 1: Локальные аудиофайлы**

```python
# TODO в main.py
def process_audio_file(
    audio_path: Path,
    transcribe_method: str,
    translate_methods: Optional[list[str]] = None
):
    """Обработка локального аудиофайла"""
    # 1. Валидация файла
    # 2. Транскрибирование
    # 3. Перевод (если нужен)
    # 4. Создание документов
    pass
```

**Неделя 2: Текстовые файлы**

```python
# TODO в translator.py или новом модуле
def process_text_file(
    text_path: Path,
    translate_methods: list[str]
):
    """Обработка текстового файла"""
    # 1. Парсинг файла (docx/md)
    # 2. Перевод
    # 3. Создание документов
    pass
```

**Неделя 3: Тестирование и документация**
- Написать интеграционные тесты
- Обновить README
- Создать примеры

---

## 💡 Советы для разработки

### Используйте Git

```bash
# Инициализация репозитория
git init

# Первый коммит
git add .
git commit -m "Initial commit: MVP v1.0"

# Создайте ветку для разработки v1.1
git checkout -b feature/v1.1-local-files
```

### Работайте с ветками

```bash
# Для каждой фичи - отдельная ветка
git checkout -b feature/audio-file-support
git checkout -b feature/text-file-support
git checkout -b feature/whisper-small
```

### Пишите тесты перед кодом (TDD)

```python
# Сначала тест
def test_process_audio_file():
    result = process_audio_file("test.mp3", "whisper_base")
    assert result is not None

# Потом реализация
def process_audio_file(path, method):
    # implementation
    pass
```

### Используйте type hints

```python
from typing import Optional, List, Tuple
from pathlib import Path

def process_video(
    url: str,
    transcribe_method: str,
    translate_methods: Optional[List[str]] = None
) -> Tuple[Path, Path]:
    """
    Обработка видео
    
    Args:
        url: URL видео
        transcribe_method: Метод транскрибирования
        translate_methods: Методы перевода
        
    Returns:
        Пути к docx и md файлам
    """
    pass
```

### Логируйте всё

```python
from src.logger import logger

logger.debug(f"Параметры: {params}")
logger.info("Начало обработки")
logger.warning("Найдены потенциальные проблемы")
logger.error("Ошибка при обработке", exc_info=True)
```

---

## 🎓 Примеры задач для начала

### Задача 1: Добавить поддержку аудиофайлов

**Шаг 1:** Обновите `main.py`

```python
# В функции validate_args добавьте проверку аудиофайлов
if args.input_audio:
    if not Path(args.input_audio).exists():
        logger.error(f"Аудиофайл не найден: {args.input_audio}")
        return False
    
    # Проверка расширения
    valid_extensions = ['.mp3', '.wav', '.m4a', '.flac']
    if not any(args.input_audio.endswith(ext) for ext in valid_extensions):
        logger.error(f"Неподдерживаемый формат аудио")
        return False
```

**Шаг 2:** Создайте функцию обработки

```python
def process_audio_file(
    audio_path: str,
    transcribe_method: str,
    translate_methods: Optional[list[str]] = None,
    with_speakers: bool = False
):
    """Обработка локального аудиофайла"""
    logger.info("=" * 60)
    logger.info("Начало обработки аудиофайла")
    logger.info("=" * 60)
    
    audio_path = Path(audio_path)
    
    # 1. Определение названия для документа
    file_title = audio_path.stem
    
    # 2. Транскрибирование
    logger.info("\n[1/3] Транскрибирование аудио...")
    transcriber = Transcriber(method=transcribe_method)
    transcription_segments = transcriber.transcribe(
        audio_path,
        language=None,
        with_speakers=with_speakers
    )
    
    # 3. Перевод (если требуется)
    translation_segments = None
    translate_method_str = ""
    
    if translate_methods:
        logger.info("\n[2/3] Перевод текста...")
        original_text = transcriber.segments_to_text(transcription_segments)
        source_lang = detect_language(original_text)
        
        if source_lang == "en":
            translate_method = translate_methods[0]
            translate_method_str = translate_method
            
            translator = Translator(method=translate_method)
            translation_segments = translator.translate_segments(
                transcription_segments,
                source_lang="en",
                target_lang="ru"
            )
        else:
            logger.info("Аудио на русском языке, перевод не требуется")
    
    # 4. Создание документов
    logger.info("\n[3/3] Создание выходных документов...")
    writer = DocumentWriter()
    docx_path, md_path = writer.create_from_segments(
        title=file_title,
        transcription_segments=transcription_segments,
        translation_segments=translation_segments,
        transcribe_method=transcribe_method,
        translate_method=translate_method_str,
        with_timestamps=True
    )
    
    logger.info("\n" + "=" * 60)
    logger.info("Обработка завершена успешно!")
    logger.info(f"Результаты сохранены:")
    logger.info(f"  - {docx_path}")
    logger.info(f"  - {md_path}")
    logger.info("=" * 60)
```

**Шаг 3:** Добавьте в main()

```python
# В функции main() после обработки URL
elif args.input_audio:
    process_audio_file(
        audio_path=args.input_audio,
        transcribe_method=args.transcribe,
        translate_methods=translate_methods,
        with_speakers=args.speakers
    )
```

**Шаг 4:** Протестируйте

```bash
# Создайте тестовый аудиофайл или используйте существующий
python -m src.main --input_audio test.mp3 --transcribe whisper_base
```

---

### Задача 2: Добавить поддержку текстовых файлов

**Шаг 1:** Создайте модуль для работы с текстом

Создайте `src/text_processor.py`:

```python
"""
Модуль для обработки текстовых файлов
"""
from pathlib import Path
from typing import Optional
from docx import Document as DocxDocument
import re

from .logger import logger


class TextProcessor:
    """Класс для обработки текстовых файлов"""
    
    def read_docx(self, path: Path) -> str:
        """
        Чтение DOCX файла
        
        Args:
            path: Путь к файлу
            
        Returns:
            Текст из файла
        """
        logger.info(f"Чтение DOCX файла: {path}")
        doc = DocxDocument(path)
        
        # Извлекаем текст из всех параграфов
        paragraphs = []
        for para in doc.paragraphs:
            text = para.text.strip()
            if text:
                paragraphs.append(text)
        
        return '\n\n'.join(paragraphs)
    
    def read_markdown(self, path: Path) -> str:
        """
        Чтение Markdown файла
        
        Args:
            path: Путь к файлу
            
        Returns:
            Текст из файла
        """
        logger.info(f"Чтение Markdown файла: {path}")
        with open(path, 'r', encoding='utf-8') as f:
            content = f.read()
        
        # Можно добавить очистку от markdown разметки если нужно
        # Но обычно NLLB справляется с разметкой
        return content
    
    def read_text_file(self, path: Path) -> str:
        """
        Универсальное чтение текстового файла
        
        Args:
            path: Путь к файлу
            
        Returns:
            Текст из файла
        """
        if path.suffix.lower() == '.docx':
            return self.read_docx(path)
        elif path.suffix.lower() in ['.md', '.markdown']:
            return self.read_markdown(path)
        elif path.suffix.lower() == '.txt':
            with open(path, 'r', encoding='utf-8') as f:
                return f.read()
        else:
            raise ValueError(f"Неподдерживаемый формат файла: {path.suffix}")
```

**Шаг 2:** Добавьте функцию обработки текста в `main.py`

```python
def process_text_file(
    text_path: str,
    translate_methods: list[str]
):
    """Обработка текстового файла"""
    logger.info("=" * 60)
    logger.info("Начало обработки текстового файла")
    logger.info("=" * 60)
    
    from .text_processor import TextProcessor
    
    text_path = Path(text_path)
    
    # 1. Чтение файла
    logger.info("\n[1/2] Чтение текстового файла...")
    processor = TextProcessor()
    text = processor.read_text_file(text_path)
    
    logger.info(f"Прочитано {len(text)} символов")
    
    # 2. Определение языка
    source_lang = detect_language(text)
    logger.info(f"Определен язык: {source_lang}")
    
    if source_lang == "ru":
        logger.error("Текст на русском языке, перевод не требуется")
        sys.exit(1)
    
    # 3. Перевод
    logger.info("\n[2/2] Перевод текста...")
    translate_method = translate_methods[0]
    translator = Translator(method=translate_method)
    translated_text = translator.translate_text(
        text,
        source_lang=source_lang,
        target_lang="ru"
    )
    
    # 4. Создание документов
    logger.info("Создание выходных документов...")
    writer = DocumentWriter()
    
    sections = [{
        'title': 'Перевод',
        'method': translate_method,
        'content': translated_text
    }]
    
    docx_path, md_path = writer.create_documents(
        title=text_path.stem,
        sections=sections
    )
    
    logger.info("\n" + "=" * 60)
    logger.info("Обработка завершена успешно!")
    logger.info(f"Результаты сохранены:")
    logger.info(f"  - {docx_path}")
    logger.info(f"  - {md_path}")
    logger.info("=" * 60)
```

**Шаг 3:** Обновите main()

```python
elif args.input_text:
    if not translate_methods:
        logger.error("Для текстового файла необходимо указать метод перевода")
        sys.exit(1)
    
    process_text_file(
        text_path=args.input_text,
        translate_methods=translate_methods
    )
```

---

### Задача 3: Добавить Whisper Small

**Обновите `transcriber.py`:**

```python
def _load_model(self):
    """Загрузка модели Whisper"""
    if self.model is not None:
        return
    
    logger.info(f"Загрузка модели Whisper ({self.method})...")
    
    if self.method == TranscribeOptions.WHISPER_BASE:
        model_name = "base"
    elif self.method == TranscribeOptions.WHISPER_SMALL:
        model_name = "small"  # Добавлено!
    elif self.method == TranscribeOptions.WHISPER_OPENAI_API:
        raise NotImplementedError("OpenAI API будет доступен в v2.0")
    else:
        raise ValueError(f"Неподдерживаемый метод: {self.method}")
    
    self.model = whisper.load_model(
        model_name,
        device=self.device,
        download_root=str(settings.WHISPER_MODEL_DIR)
    )
    logger.info("Модель загружена успешно")
```

---

## 🐛 Debugging советы

### Используйте IPython для отладки

```python
# Добавьте в код где нужно остановиться
import IPython; IPython.embed()
```

### Логируйте промежуточные состояния

```python
logger.debug(f"Segments count: {len(segments)}")
logger.debug(f"First segment: {segments[0]}")
```

### Используйте breakpoints в Cursor

Поставьте breakpoint (F9) и запустите в режиме отладки (F5)

---

## 📊 Мониторинг прогресса

### Checklist для v1.1

- [ ] Обработка локальных аудиофайлов
  - [ ] Валидация файлов
  - [ ] Поддержка mp3, wav, m4a, flac
  - [ ] Тесты
  - [ ] Документация

- [ ] Обработка текстовых файлов
  - [ ] TextProcessor модуль
  - [ ] Поддержка docx
  - [ ] Поддержка markdown
  - [ ] Тесты
  - [ ] Документация

- [ ] Whisper Small модель
  - [ ] Добавлен в TranscribeOptions
  - [ ] Загрузка модели
  - [ ] Тесты
  - [ ] Сравнение качества

- [ ] Улучшения
  - [ ] Цветной вывод логов
  - [ ] Прогресс-бары для всех операций
  - [ ] Оценка времени обработки
  - [ ] Graceful shutdown (Ctrl+C)

---

## 📝 Финальный чеклист перед началом

✅ **Проверьте что всё готово:**

- [ ] Структура проекта создана
- [ ] Весь код скопирован из артефактов
- [ ] Виртуальное окружение создано и активировано
- [ ] FFmpeg установлен
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Тесты запускаются (`pytest tests/`)
- [ ] Первый реальный тест прошел успешно
- [ ] Git репозиторий инициализирован
- [ ] `.env` файл создан (скопировать из `.env.example`)
- [ ] Cursor настроен (interpreter, extensions)

---

## 🚀 Начинайте разработку!

**Вы готовы!** Теперь у вас есть:

1. ✅ Полностью рабочий MVP (v1.0)
2. ✅ Чистая архитектура для расширения
3. ✅ Понятные задачи для v1.1
4. ✅ Примеры кода для реализации
5. ✅ Инструменты для разработки

**Следующие шаги:**

1. Начните с самой простой задачи (например, Whisper Small)
2. Напишите тест → Напишите код → Убедитесь что тест проходит
3. Коммитьте часто: `git commit -m "Add whisper small support"`
4. Используйте Cursor AI для помощи
5. Обновляйте документацию по мере разработки

**Удачи в разработке! 🎉**

---

## 💬 Помощь и поддержка

Если что-то не работает:

1. Проверьте логи в `logs/`
2. Запустите с `LOG_LEVEL=DEBUG`
3. Проверьте FAQ.md
4. Создайте issue с подробным описанием проблемы

---

**Последнее обновление:** Октябрь 2024